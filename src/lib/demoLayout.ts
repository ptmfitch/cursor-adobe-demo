/**
 * DEMO: Set to 0 so hint 3 stays clipped.
 * Fix to FEATURE_SECTION_GAP_STANDARD_PX per demo-standards.mdc.
 */
export const FEATURE_SECTION_GAP_PX = 0;

export const FEATURE_SECTION_GAP_STANDARD_PX = 24;

/** DEMO: Panel inset imbalance — fix to PANEL_INSET_STANDARD_PX per rules. */
export const PANEL_INSET_END_PX = 32;

export const PANEL_INSET_STANDARD_PX = 24;

export const PAGE_MAX_WIDTH_PX = 960;

export const HERO_MARK_SIZE_PX = 56;

export const CONTENT_SECTION_GAP_PX = 48;
