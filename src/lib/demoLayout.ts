/** Standard gap between feature cards — see .cursor/rules/demo-standards.mdc */
export const FEATURE_CARD_GAP_PX = 12;

/**
 * DEMO: Set to 0 so reveal hint 2 is clipped/hidden.
 * Fix to FEATURE_SECTION_GAP_STANDARD_PX per project rules.
 */
export const FEATURE_SECTION_GAP_PX = 0;

export const FEATURE_SECTION_GAP_STANDARD_PX = 24;

export const PAGE_MAX_WIDTH_PX = 768;

export const HERO_LOGO_SIZE_PX = 48;
